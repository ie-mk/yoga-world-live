import styled from 'styled-components';

const MapWrapper = styled.div`
  margin-top: -2px;
  height: calc(100vh - 68px);
  width: 100%;
  position: sticky;
  top: 67px;
`;

export default {
  MapWrapper,
};
